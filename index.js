const num1 = 31;
const num2 = 2;

let multiply = `${num1}` * `${num2}`;

let random = Math.floor(Math.random() + 2);

const num3 = 8
const num4 = 2

let mod = `${num3}` / `${num4}`;

let max = Math.max(5, 10, 15, 20);